{% extends 'base.html.twig' %}

{% block title %}Hello MerchController!{% endblock %}

{% block current_css %}
    <link rel="stylesheet" href="{{ asset('public/build/merch.css') }}">
{% endblock %}

{% block body %}

{% for message in app.flashes('success') %}
<div class="alert-success">
    {{ message }}
</div>
{% endfor %}

<main


{% for product in products %}
    <div class="merch">
        <a href="{{path ('home_editProduct', {id: product.id}) }}">{{ product.name }}</a>
        <p>{{ product.name }}</p>
    </div>
    
{% endfor %}

			<a href="{{ path('home_createProduct') }}">
				<button type="button" id="button-create">Create</button>
			</a>
</main>
{% endblock %}
