{% extends 'base.html.twig' %}

{% block title %}Hello MerchController!{% endblock %}

{% block current_css %}
    <link rel="stylesheet" href="{{ asset('public/build/merch.css') }}">
{% endblock %}

{% block body %}

{% for message in app.flashes('success') %}
<div class="alert-success">
    {{ message }}
</div>
{% endfor %}



<div class="product">
    {% for product in products %}
    <div class="shadow">
    <a href="productsell"
        <div class="merch">
            <a id="productName" href="{{path ('merch_readProduct', {id: product.id}) }}">
            <p>{{ product.name }}</p>
            <div class="productImg"><img id="productimg" src="{{ vich_uploader_asset(product,'imageFile') }}"></div>
            </a>
    </a>        

    			<a href="{{ path('home_editProduct', {'id': product.id}) }}">
				<button type="button" id="button-modify">Modify</button>
			</a>
        </div>
    </div>
        
    {% endfor %}
</div>
			<a href="{{ path('home_createProduct') }}">
				<button type="button" id="button-create">Create</button>
			</a>

{% endblock %}
